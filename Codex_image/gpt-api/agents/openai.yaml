interface:
  display_name: "GPT APIMart API"
  short_description: "Generate images with GPT Image 2 through APIMart"
  default_prompt: "Use $gpt-api to generate or edit an image with APIMart GPT Image 2."
policy:
  allow_implicit_invocation: false
