interface:
  display_name: "建筑特写至全貌动态组装视频"
  short_description: "将建筑外观、构造节点、室内、露台、庭院等多张参考图编排为从特写到全貌的动态拆解组装展示提示词。"
  default_prompt: "Use $architectural-assembly-reveal to create a professional video prompt from my supplied references and requirements."
policy:
  allow_implicit_invocation: true

